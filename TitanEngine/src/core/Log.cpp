// Este projeto é feito por IA e só o prompt é feito por um humano.
#include "Log.hpp"

// No current implementation details needed in .cpp as it's mostly template/static based, 
// but we might add static member initializers if needed later.
// For now, this is a placeholder to satisfy the build system.
